Token Analysis Report
==================================================
Token: Pepe Popo (0x18E46C4ceA940F41D5668281bc3d253A59424D19)
Chain: ETH
Contract Analysis
-----------------
Verified: OK!
Owner Address: None
Creator Address: None
Is Proxy: No
Is sellable (no honeypot): Yes
Is owner hardcoded: No

Code Analysis Findings:
WARNING: Ownership Renounced
  Code Snippet:
    function renounceownership() public virtual onlyowner {
        emit ownershiptransferred(_owner, address(0));
        _owner = address(0);
    }

WARNING: Delayed Trading Detected
  Code Snippet:
    function _transfer(address from, address to, uint256 amount) private {
        require(from != address(0), "erc20: transfer from the zero address");
        require(to != address(0), "erc20: transfer to the zero address");
        require(amount > 0, "transfer amount must be greater than zero");
        uint256 taxamount=0;
        if (from != owner() && to != owner()) {
            require(!bots[from] && !bots[to]);

            if(_buycount==0){
                taxamount = amount.mul((_buycount>_reducebuytaxat)?_finalbuytax:_initialbuytax).div(100);
            }
            if(_buycount>0){
                taxamount = amount.mul(_transfertax).div(100);
            }

            if (from == uniswapv2pair && to != address(uniswapv2router) && ! _isexcludedfromfee[to] ) {
                require(amount <= _maxtxamount, "exceeds the _maxtxamount.");
                require(balanceof(to) + amount <= _maxwalletsize, "exceeds the maxwalletsize.");
                taxamount = amount.mul((_buycount>_reducebuytaxat)?_finalbuytax:_initialbuytax).div(100);
                _buycount++;
            }

            if(to == uniswapv2pair && from!= address(this) ){
                taxamount = amount.mul((_buycount>_reduceselltaxat)?_finalselltax:_initialselltax).div(100);
            }

            uint256 contracttokenbalance = balanceof(address(this));
            if (!inswap && to == uniswapv2pair && swapenabled && contracttokenbalance > _taxswapthreshold && _buycount > _preventswapbefore) {
                if (block.number > lastsellblock) {
                    sellcount = 0;
                }
                require(sellcount < 3, "only 3 sells per block!");
                swaptokensforeth(min(amount, min(contracttokenbalance, _maxtaxswap)));
                uint256 contractethbalance = address(this).balance;
                if (contractethbalance > 0) {
                    sendethtofee(address(this).balance);
                }
                sellcount++;
                lastsellblock = block.number;
            }
        }

        if(taxamount>0){
          _balances[address(this)]=_balances[address(this)].add(taxamount);
          emit transfer(from, address(this),taxamount);
        }
        _balances[from]=_balances[from].sub(amount);
        _balances[to]=_balances[to].add(amount.sub(taxamount));
        emit transfer(from, to, amount.sub(taxamount));
    }

WARNING: High Tax Detected
  Code Snippet:
    function swapexacttokensforethsupportingfeeontransfertokens(
        uint amountin,

  Code Snippet:
    function _transfer(address from, address to, uint256 amount) private {
        require(from != address(0), "erc20: transfer from the zero address");
        require(to != address(0), "erc20: transfer to the zero address");
        require(amount > 0, "transfer amount must be greater than zero");
        uint256 taxamount=0;
        if (from != owner() && to != owner()) {
            require(!bots[from] && !bots[to]);

            if(_buycount==0){
                taxamount = amount.mul((_buycount>_reducebuytaxat)?_finalbuytax:_initialbuytax).div(100);
            }
            if(_buycount>0){
                taxamount = amount.mul(_transfertax).div(100);
            }

            if (from == uniswapv2pair && to != address(uniswapv2router) && ! _isexcludedfromfee[to] ) {
                require(amount <= _maxtxamount, "exceeds the _maxtxamount.");
                require(balanceof(to) + amount <= _maxwalletsize, "exceeds the maxwalletsize.");
                taxamount = amount.mul((_buycount>_reducebuytaxat)?_finalbuytax:_initialbuytax).div(100);
                _buycount++;
            }

            if(to == uniswapv2pair && from!= address(this) ){
                taxamount = amount.mul((_buycount>_reduceselltaxat)?_finalselltax:_initialselltax).div(100);
            }

            uint256 contracttokenbalance = balanceof(address(this));
            if (!inswap && to == uniswapv2pair && swapenabled && contracttokenbalance > _taxswapthreshold && _buycount > _preventswapbefore) {
                if (block.number > lastsellblock) {
                    sellcount = 0;
                }
                require(sellcount < 3, "only 3 sells per block!");
                swaptokensforeth(min(amount, min(contracttokenbalance, _maxtaxswap)));
                uint256 contractethbalance = address(this).balance;
                if (contractethbalance > 0) {
                    sendethtofee(address(this).balance);
                }
                sellcount++;
                lastsellblock = block.number;
            }
        }

        if(taxamount>0){
          _balances[address(this)]=_balances[address(this)].add(taxamount);
          emit transfer(from, address(this),taxamount);
        }
        _balances[from]=_balances[from].sub(amount);
        _balances[to]=_balances[to].add(amount.sub(taxamount));
        emit transfer(from, to, amount.sub(taxamount));
    }

  Code Snippet:
    function swaptokensforeth(uint256 tokenamount) private locktheswap {
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = uniswapv2router.weth();
        _approve(address(this), address(uniswapv2router), tokenamount);
        uniswapv2router.swapexacttokensforethsupportingfeeontransfertokens(
            tokenamount,
            0,
            path,
            address(this),
            block.timestamp
        );
    }

  Code Snippet:
    function sendethtofee(uint256 amount) private {
        _taxwallet.transfer(amount);
    }

  Code Snippet:
    function opentrading() external onlyowner() {
        require(!tradingopen,"trading is already open");
        uniswapv2router = iuniswapv2router02(0x7a250d5630b4cf539739df2c5dacb4c659f2488d);
        _approve(address(this), address(uniswapv2router), _ttotal);
        uniswapv2pair = iuniswapv2factory(uniswapv2router.factory()).createpair(address(this), uniswapv2router.weth());
        uniswapv2router.addliquidityeth{value: address(this).balance}(address(this),balanceof(address(this)),0,0,owner(),block.timestamp);
        ierc20(uniswapv2pair).approve(address(uniswapv2router), type(uint).max);
        swapenabled = true;
        tradingopen = true;
    }

  Code Snippet:
    function reducefee(uint256 _newfee) external{
      require(_msgsender()==_taxwallet);
      require(_newfee<=_finalbuytax && _newfee<=_finalselltax);
      _finalbuytax=_newfee;
      _finalselltax=_newfee;
    }

  Code Snippet:
    function manualswap() external {
        require(_msgsender()==_taxwallet);
        uint256 tokenbalance=balanceof(address(this));
        if(tokenbalance>0){
          swaptokensforeth(tokenbalance);
        }
        uint256 ethbalance=address(this).balance;
        if(ethbalance>0){
          sendethtofee(ethbalance);
        }
    }

WARNING: Trading Disabled Possible
  Code Snippet:
    function opentrading() external onlyowner() {
        require(!tradingopen,"trading is already open");
        uniswapv2router = iuniswapv2router02(0x7a250d5630b4cf539739df2c5dacb4c659f2488d);
        _approve(address(this), address(uniswapv2router), _ttotal);
        uniswapv2pair = iuniswapv2factory(uniswapv2router.factory()).createpair(address(this), uniswapv2router.weth());
        uniswapv2router.addliquidityeth{value: address(this).balance}(address(this),balanceof(address(this)),0,0,owner(),block.timestamp);
        ierc20(uniswapv2pair).approve(address(uniswapv2router), type(uint).max);
        swapenabled = true;
        tradingopen = true;
    }


Holder Analysis
--------------
Total Unique Holders: 168
⚠️ Owner information is not available (possibly hidden or unverified)
⚠️ Creator information is not available (possibly hidden or unverified)
Holders >5%: 2
⚠️ Some holders exceed 5% of supply

Top 10 Token Holders:
  1. 0xe7afb1a84569d6d0056bb9022f772a267f9968dc — 46,360,813,537,185,660,928 tokens (66.78% of circulating supply)
  2. 0xed5a36346554f6a7cfdc3a3f8e000f86de2a7d72 — 3,771,826,728,791,896,064 tokens (5.43% of circulating supply)
  3. 0x92d3f49a8720047b62ec8945d051a7b883a0e459 — 2,645,565,234,665,637,376 tokens (3.81% of circulating supply)
  4. 0xa9bf1cf0c92424fbbe7c1cf5f8a1e8c8ae52da6b — 1,674,372,779,605,296,896 tokens (2.41% of circulating supply)
  5. 0x194f0fe6d4dfbf198868b1b507cd74122fbfebcc — 1,076,250,534,343,428,608 tokens (1.55% of circulating supply)
  6. 0x77c519b3625946142d2e903aec4a8b0f525bba1c — 935,317,325,878,871,040 tokens (1.35% of circulating supply)
  7. 0x8aa00e994746e5dd8f35217c3a59d3aac4c14a69 — 810,275,626,724,376,448 tokens (1.17% of circulating supply)
  8. 0x242c4c09bb252107daf70842e2af82982c5fe285 — 800,000,000,043,845,248 tokens (1.15% of circulating supply)
  9. 0x2bc8f064004f43e0a0aa27af56815f028ee71255 — 530,149,727,046,278,080 tokens (0.76% of circulating supply)
  10. 0x74e038864ee45c6f97ae0602bb4b807fed49302a — 499,361,279,210,841,344 tokens (0.72% of circulating supply)

Top 10 Total Balance: 59,103,932,773,496,132,032 tokens
Top 10 Share of Circulating Supply: 85.14%
Top 10 Share of Total Supply: 85.14%
✅ Top 10 holders control LESS than 70% of circulating supply

Liquidity Analysis
-----------------
Market Cap: $5,231,395,229,030,471,958,528.00
Liquidity: $6,987.37
Liquidity/MCap Ratio: 0.0000
Token Volume: 61083869747.5970
USD Volume: 4603195977647.6172
24h Volume/Liquidity Ratio: 658787724.8811
Percentage of liquidity locked: 100.0000
Was 95% of liquidity locked for more than 15 days?: False
Secure
Creator owns under 5% of LP tokens: True (0.0)
Total supply of LP tokens: 6.223854111400748
LP holders count: 2

Liquidity holders for 0x18E46C4ceA940F41D5668281bc3d253A59424D19, (Pepe Popo)

0x000000000000000000000000000000000000dead holds 6.2238541114007475 LP tokens

0x0000000000000000000000000000000000000000 holds 1e-15 LP tokens

Security Analysis
-----------------
⚠️ Suspicious URL(s) found in the contract.
  URL: https://t.me/popotokeneth, Reason: metamask recovery phrase phishing
  URL: https://twitter.com/PopoTokenETH, Reason: metamask recovery phrase phishing
  URL: https://popotokeneth.com/, Reason: metamask recovery phrase phishing

Lifecycle Analysis
-------------
Token Age: 483.92 days
Time to First Trade: 0.00 hours
Blocks to First Trade: 0
Token Creation Date: 2024-04-07T15:03:23
Last Active: 2025-07-26T13:32:11
Days Since Last Activity: 8.97985776667824 days
Last Transaction Hash: 0xa5a69313d8f7948ff29e9132131a21bf4dfb26129c000f014740da3559cfb37d
