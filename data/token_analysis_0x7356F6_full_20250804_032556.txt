Token Analysis Report
==================================================
Token: MAJA Token (0x7356F6D4D9802aC66CE4349B99Aedc80B1AC65E2)
Chain: BSC
Contract Analysis
-----------------
Verified: OK!
Owner Address: None
Creator Address: None
Is Proxy: No
Is sellable (no honeypot): Yes
Is owner hardcoded: No

Code Analysis Findings:
WARNING: Mint Function Detected
  Code Snippet:
    function setasairdropaccount(address newairdropaddress) public onlyowner notaddress0(newairdropaddress) {
        _airdropaddress = newairdropaddress;
    }

  Code Snippet:
    function isairdrop(address account) public view returns (bool) {
        return _isairdrop[account];
    }

  Code Snippet:
    function totalairdrop() public view returns (uint256) {
        return _tairdroptotal;
    }

  Code Snippet:
	function updatefee(uint256 _txfee,uint256 _burnfee,uint256 _airdropfee) onlyowner() public{
        _tax_fee = _txfee* 1;
        _burn_fee = _burnfee * 1;
		_airdrop_fee = _airdropfee* 1;
		orig_tax_fee = _tax_fee;
		orig_burn_fee = _burn_fee;
		orig_airdrop_fee = _airdrop_fee;
	}

  Code Snippet:
    function _transfer(address sender, address recipient, uint256 amount) private {
        require(sender != address(0), "token20: transfer from the zero address");
        require(recipient != address(0), "token20: transfer to the zero address");
        require(amount > 0, "transfer amount must be greater than zero");

        // remove fees for transfers to and from airdrop account or to excluded account
        bool takefee = true;
        if (_isairdrop[sender] || _isairdrop[recipient] || _isexcluded[recipient]) {
            takefee = false;
        }

        if (!takefee) removeallfee();
        
        
        if (_isexcluded[sender] && !_isexcluded[recipient]) {
            _transferfromexcluded(sender, recipient, amount);
        } else if (!_isexcluded[sender] && _isexcluded[recipient]) {
            _transfertoexcluded(sender, recipient, amount);
        } else if (!_isexcluded[sender] && !_isexcluded[recipient]) {
            _transferstandard(sender, recipient, amount);
        } else if (_isexcluded[sender] && _isexcluded[recipient]) {
            _transferbothexcluded(sender, recipient, amount);
        } else {
            _transferstandard(sender, recipient, amount);
        }

        if (!takefee) restoreallfee();
    }

  Code Snippet:
    function _transferstandard(address sender, address recipient, uint256 tamount) private {
        uint256 currentrate =  _getrate();
        (uint256 ramount, uint256 rtransferamount, uint256 rfee, uint256 ttransferamount, uint256 tfee, uint256 tburn, uint256 tairdrop) = _getvalues(tamount);
        uint256 rburn =  tburn.mul(currentrate);
        uint256 rairdrop = tairdrop.mul(currentrate);     
        _standardtransfercontent(sender, recipient, ramount, rtransferamount);
        _sendtoairdrop(tairdrop, sender);
        _reflectfee(rfee, rburn, rairdrop, tfee, tburn, tairdrop);
        emit transfer(sender, recipient, ttransferamount);
    }

  Code Snippet:
    function _transfertoexcluded(address sender, address recipient, uint256 tamount) private {
        uint256 currentrate =  _getrate();
        (uint256 ramount, uint256 rtransferamount, uint256 rfee, uint256 ttransferamount, uint256 tfee, uint256 tburn, uint256 tairdrop) = _getvalues(tamount);
        uint256 rburn =  tburn.mul(currentrate);
        uint256 rairdrop = tairdrop.mul(currentrate);
        _excludedfromtransfercontent(sender, recipient, ttransferamount, ramount, rtransferamount);        
        _sendtoairdrop(tairdrop, sender);
        _reflectfee(rfee, rburn, rairdrop, tfee, tburn, tairdrop);
        emit transfer(sender, recipient, ttransferamount);
    }

  Code Snippet:
    function _transferfromexcluded(address sender, address recipient, uint256 tamount) private {
        uint256 currentrate =  _getrate();
        (uint256 ramount, uint256 rtransferamount, uint256 rfee, uint256 ttransferamount, uint256 tfee, uint256 tburn, uint256 tairdrop) = _getvalues(tamount);
        uint256 rburn =  tburn.mul(currentrate);
        uint256 rairdrop = tairdrop.mul(currentrate);
        _excludedtotransfercontent(sender, recipient, tamount, ramount, rtransferamount);
        _sendtoairdrop(tairdrop, sender);
        _reflectfee(rfee, rburn, rairdrop, tfee, tburn, tairdrop);
        emit transfer(sender, recipient, ttransferamount);
    }

  Code Snippet:
    function _transferbothexcluded(address sender, address recipient, uint256 tamount) private {
        uint256 currentrate =  _getrate();
        (uint256 ramount, uint256 rtransferamount, uint256 rfee, uint256 ttransferamount, uint256 tfee, uint256 tburn, uint256 tairdrop) = _getvalues(tamount);
        uint256 rburn =  tburn.mul(currentrate);
        uint256 rairdrop = tairdrop.mul(currentrate);    
        _bothtransfercontent(sender, recipient, tamount, ramount, ttransferamount, rtransferamount);  
        _sendtoairdrop(tairdrop, sender);
        _reflectfee(rfee, rburn, rairdrop, tfee, tburn, tairdrop);
        emit transfer(sender, recipient, ttransferamount);
    }

  Code Snippet:
    function _reflectfee(uint256 rfee, uint256 rburn, uint256 rairdrop, uint256 tfee, uint256 tburn, uint256 tairdrop) private {
        _rtotal = _rtotal.sub(rfee).sub(rburn).sub(rairdrop);
        _tfeetotal = _tfeetotal.add(tfee);
        _tburntotal = _tburntotal.add(tburn);
        _tairdroptotal = _tairdroptotal.add(tairdrop);
        _ttotal = _ttotal.sub(tburn);
		emit transfer(address(this), _burnaddress, tburn);
    }

  Code Snippet:
    function _getvalues(uint256 tamount) private view returns (uint256, uint256, uint256, uint256, uint256, uint256, uint256) {
        (uint256 tfee, uint256 tburn, uint256 tairdrop) = _gettbasics(tamount, _tax_fee, _burn_fee, _airdrop_fee);
        uint256 ttransferamount = getttransferamount(tamount, tfee, tburn, tairdrop);
        uint256 currentrate =  _getrate();
        (uint256 ramount, uint256 rfee) = _getrbasics(tamount, tfee, currentrate);
        uint256 rtransferamount = _getrtransferamount(ramount, rfee, tburn, tairdrop, currentrate);
        return (ramount, rtransferamount, rfee, ttransferamount, tfee, tburn, tairdrop);
    }

  Code Snippet:
    function _gettbasics(uint256 tamount, uint256 taxfee, uint256 burnfee, uint256 airdropfee) private view returns (uint256, uint256, uint256) {
        uint256 tfee = ((tamount.mul(taxfee)).div(_granularity)).div(100);
        uint256 tburn = ((tamount.mul(burnfee)).div(_granularity)).div(100);
        uint256 tairdrop = ((tamount.mul(airdropfee)).div(_granularity)).div(100);
        return (tfee, tburn, tairdrop);
    }

  Code Snippet:
    function getttransferamount(uint256 tamount, uint256 tfee, uint256 tburn, uint256 tairdrop) private pure returns (uint256) {
        return tamount.sub(tfee).sub(tburn).sub(tairdrop);
    }

  Code Snippet:
    function _getrtransferamount(uint256 ramount, uint256 rfee, uint256 tburn, uint256 tairdrop, uint256 currentrate) private pure returns (uint256) {
        uint256 rburn = tburn.mul(currentrate);
        uint256 rairdrop = tairdrop.mul(currentrate);
        uint256 rtransferamount = ramount.sub(rfee).sub(rburn).sub(rairdrop);
        return rtransferamount;
    }

  Code Snippet:
    function _sendtoairdrop(uint256 tairdrop, address sender) private {
        uint256 currentrate = _getrate();
        uint256 rairdrop = tairdrop.mul(currentrate);
        _rowned[_airdropaddress] = _rowned[_airdropaddress].add(rairdrop);
        _towned[_airdropaddress] = _towned[_airdropaddress].add(tairdrop);
        emit transfer(sender, _airdropaddress, tairdrop);
    }

  Code Snippet:
    function removeallfee() private {
        if(_tax_fee == 0 && _burn_fee == 0 && _airdrop_fee == 0) return;
        
        orig_tax_fee = _tax_fee;
        orig_burn_fee = _burn_fee;
        orig_airdrop_fee = _airdrop_fee;
        
        _tax_fee = 0;
        _burn_fee = 0;
        _airdrop_fee = 0;
    }

  Code Snippet:
    function restoreallfee() private {
        _tax_fee = orig_tax_fee;
        _burn_fee = orig_burn_fee;
        _airdrop_fee = orig_airdrop_fee;
    }

WARNING: Ownership Renounced
  Code Snippet:
    function renounceownership() public virtual onlyowner {
        emit ownershiptransferred(_owner, address(0));
        _owner = address(0);
    }

WARNING: High Tax Detected
  Code Snippet:
    function totalfees() public view returns (uint256) {
        return _tfeetotal;
    }

  Code Snippet:
    function deliver(uint256 tamount) public {
        address sender = _msgsender();
        require(!_isexcluded[sender], "excluded addresses cannot call this function");
        (uint256 ramount,,,,,,) = _getvalues(tamount);
        _rowned[sender] = _rowned[sender].sub(ramount);
        _rtotal = _rtotal.sub(ramount);
        _tfeetotal = _tfeetotal.add(tamount);
    }

  Code Snippet:
    function reflectionfromtoken(uint256 tamount, bool deducttransferfee) public view returns(uint256) {
        require(tamount <= _ttotal, "amount must be less than supply");
        if (!deducttransferfee) {
            (uint256 ramount,,,,,,) = _getvalues(tamount);
            return ramount;
        } else {
            (,uint256 rtransferamount,,,,,) = _getvalues(tamount);
            return rtransferamount;
        }
    }

  Code Snippet:
	function updatefee(uint256 _txfee,uint256 _burnfee,uint256 _airdropfee) onlyowner() public{
        _tax_fee = _txfee* 1;
        _burn_fee = _burnfee * 1;
		_airdrop_fee = _airdropfee* 1;
		orig_tax_fee = _tax_fee;
		orig_burn_fee = _burn_fee;
		orig_airdrop_fee = _airdrop_fee;
	}

  Code Snippet:
    function _transfer(address sender, address recipient, uint256 amount) private {
        require(sender != address(0), "token20: transfer from the zero address");
        require(recipient != address(0), "token20: transfer to the zero address");
        require(amount > 0, "transfer amount must be greater than zero");

        // remove fees for transfers to and from airdrop account or to excluded account
        bool takefee = true;
        if (_isairdrop[sender] || _isairdrop[recipient] || _isexcluded[recipient]) {
            takefee = false;
        }

        if (!takefee) removeallfee();
        
        
        if (_isexcluded[sender] && !_isexcluded[recipient]) {
            _transferfromexcluded(sender, recipient, amount);
        } else if (!_isexcluded[sender] && _isexcluded[recipient]) {
            _transfertoexcluded(sender, recipient, amount);
        } else if (!_isexcluded[sender] && !_isexcluded[recipient]) {
            _transferstandard(sender, recipient, amount);
        } else if (_isexcluded[sender] && _isexcluded[recipient]) {
            _transferbothexcluded(sender, recipient, amount);
        } else {
            _transferstandard(sender, recipient, amount);
        }

        if (!takefee) restoreallfee();
    }

  Code Snippet:
    function _transferstandard(address sender, address recipient, uint256 tamount) private {
        uint256 currentrate =  _getrate();
        (uint256 ramount, uint256 rtransferamount, uint256 rfee, uint256 ttransferamount, uint256 tfee, uint256 tburn, uint256 tairdrop) = _getvalues(tamount);
        uint256 rburn =  tburn.mul(currentrate);
        uint256 rairdrop = tairdrop.mul(currentrate);     
        _standardtransfercontent(sender, recipient, ramount, rtransferamount);
        _sendtoairdrop(tairdrop, sender);
        _reflectfee(rfee, rburn, rairdrop, tfee, tburn, tairdrop);
        emit transfer(sender, recipient, ttransferamount);
    }

  Code Snippet:
    function _transfertoexcluded(address sender, address recipient, uint256 tamount) private {
        uint256 currentrate =  _getrate();
        (uint256 ramount, uint256 rtransferamount, uint256 rfee, uint256 ttransferamount, uint256 tfee, uint256 tburn, uint256 tairdrop) = _getvalues(tamount);
        uint256 rburn =  tburn.mul(currentrate);
        uint256 rairdrop = tairdrop.mul(currentrate);
        _excludedfromtransfercontent(sender, recipient, ttransferamount, ramount, rtransferamount);        
        _sendtoairdrop(tairdrop, sender);
        _reflectfee(rfee, rburn, rairdrop, tfee, tburn, tairdrop);
        emit transfer(sender, recipient, ttransferamount);
    }

  Code Snippet:
    function _transferfromexcluded(address sender, address recipient, uint256 tamount) private {
        uint256 currentrate =  _getrate();
        (uint256 ramount, uint256 rtransferamount, uint256 rfee, uint256 ttransferamount, uint256 tfee, uint256 tburn, uint256 tairdrop) = _getvalues(tamount);
        uint256 rburn =  tburn.mul(currentrate);
        uint256 rairdrop = tairdrop.mul(currentrate);
        _excludedtotransfercontent(sender, recipient, tamount, ramount, rtransferamount);
        _sendtoairdrop(tairdrop, sender);
        _reflectfee(rfee, rburn, rairdrop, tfee, tburn, tairdrop);
        emit transfer(sender, recipient, ttransferamount);
    }

  Code Snippet:
    function _transferbothexcluded(address sender, address recipient, uint256 tamount) private {
        uint256 currentrate =  _getrate();
        (uint256 ramount, uint256 rtransferamount, uint256 rfee, uint256 ttransferamount, uint256 tfee, uint256 tburn, uint256 tairdrop) = _getvalues(tamount);
        uint256 rburn =  tburn.mul(currentrate);
        uint256 rairdrop = tairdrop.mul(currentrate);    
        _bothtransfercontent(sender, recipient, tamount, ramount, ttransferamount, rtransferamount);  
        _sendtoairdrop(tairdrop, sender);
        _reflectfee(rfee, rburn, rairdrop, tfee, tburn, tairdrop);
        emit transfer(sender, recipient, ttransferamount);
    }

  Code Snippet:
    function _reflectfee(uint256 rfee, uint256 rburn, uint256 rairdrop, uint256 tfee, uint256 tburn, uint256 tairdrop) private {
        _rtotal = _rtotal.sub(rfee).sub(rburn).sub(rairdrop);
        _tfeetotal = _tfeetotal.add(tfee);
        _tburntotal = _tburntotal.add(tburn);
        _tairdroptotal = _tairdroptotal.add(tairdrop);
        _ttotal = _ttotal.sub(tburn);
		emit transfer(address(this), _burnaddress, tburn);
    }

  Code Snippet:
    function _getvalues(uint256 tamount) private view returns (uint256, uint256, uint256, uint256, uint256, uint256, uint256) {
        (uint256 tfee, uint256 tburn, uint256 tairdrop) = _gettbasics(tamount, _tax_fee, _burn_fee, _airdrop_fee);
        uint256 ttransferamount = getttransferamount(tamount, tfee, tburn, tairdrop);
        uint256 currentrate =  _getrate();
        (uint256 ramount, uint256 rfee) = _getrbasics(tamount, tfee, currentrate);
        uint256 rtransferamount = _getrtransferamount(ramount, rfee, tburn, tairdrop, currentrate);
        return (ramount, rtransferamount, rfee, ttransferamount, tfee, tburn, tairdrop);
    }

  Code Snippet:
    function _gettbasics(uint256 tamount, uint256 taxfee, uint256 burnfee, uint256 airdropfee) private view returns (uint256, uint256, uint256) {
        uint256 tfee = ((tamount.mul(taxfee)).div(_granularity)).div(100);
        uint256 tburn = ((tamount.mul(burnfee)).div(_granularity)).div(100);
        uint256 tairdrop = ((tamount.mul(airdropfee)).div(_granularity)).div(100);
        return (tfee, tburn, tairdrop);
    }

  Code Snippet:
    function getttransferamount(uint256 tamount, uint256 tfee, uint256 tburn, uint256 tairdrop) private pure returns (uint256) {
        return tamount.sub(tfee).sub(tburn).sub(tairdrop);
    }

  Code Snippet:
    function _getrbasics(uint256 tamount, uint256 tfee, uint256 currentrate) private pure returns (uint256, uint256) {
        uint256 ramount = tamount.mul(currentrate);
        uint256 rfee = tfee.mul(currentrate);
        return (ramount, rfee);
    }

  Code Snippet:
    function _getrtransferamount(uint256 ramount, uint256 rfee, uint256 tburn, uint256 tairdrop, uint256 currentrate) private pure returns (uint256) {
        uint256 rburn = tburn.mul(currentrate);
        uint256 rairdrop = tairdrop.mul(currentrate);
        uint256 rtransferamount = ramount.sub(rfee).sub(rburn).sub(rairdrop);
        return rtransferamount;
    }

  Code Snippet:
    function removeallfee() private {
        if(_tax_fee == 0 && _burn_fee == 0 && _airdrop_fee == 0) return;
        
        orig_tax_fee = _tax_fee;
        orig_burn_fee = _burn_fee;
        orig_airdrop_fee = _airdrop_fee;
        
        _tax_fee = 0;
        _burn_fee = 0;
        _airdrop_fee = 0;
    }

  Code Snippet:
    function restoreallfee() private {
        _tax_fee = orig_tax_fee;
        _burn_fee = orig_burn_fee;
        _airdrop_fee = orig_airdrop_fee;
    }

  Code Snippet:
    function _gettaxfee() private view returns(uint256) {
        return _tax_fee;
    }


Holder Analysis
--------------
Total Unique Holders: 2
⚠️ Owner information is not available (possibly hidden or unverified)
⚠️ Creator information is not available (possibly hidden or unverified)
Holders >5%: 2
⚠️ Some holders exceed 5% of supply

Top 10 Token Holders:
  1. 0x000000000000000000000000000000000000dead — 16 tokens (69.57% of circulating supply)
  2. 0xdc53e2d2447504abec140b37d5ed4977ddbb0c4c — 2 tokens (8.70% of circulating supply)

Top 10 Total Balance: 18 tokens
Top 10 Share of Circulating Supply: 78.26%
Top 10 Share of Total Supply: 62.07%
✅ Top 10 holders control LESS than 70% of circulating supply

Liquidity Analysis
-----------------
Market Cap: $12,549,826,572,483,338.00
Liquidity: $0.00
Liquidity/MCap Ratio: 0.0000
Token Volume: 61212425605007.7344
USD Volume: 33400231540168933749184004096.0000
24h Volume/Liquidity Ratio: 15303106401251932552660346994688.0000
Percentage of liquidity locked: 95.5200
Was 95% of liquidity locked for more than 15 days?: False
Secure
Creator owns under 5% of LP tokens: True (0.0)
Total supply of LP tokens: 14.805656029886746
LP holders count: 6

Liquidity holders for 0x7356F6D4D9802aC66CE4349B99Aedc80B1AC65E2, (MAJA Token)

0x3f4d6bf08cb7a003488ef082102c2e6418a4551e holds 14.1421 LP tokens

0x3536b0152c91e60535508690a650c10bf09fe857 holds 0.5297492780079719 LP tokens

0x0ed943ce24baebf257488771759f9bf482c39706 holds 0.051105219712359526 LP tokens

0x23d3f553042596abeba01e3edf373904274bdc8a holds 0.049209386247635205 LP tokens

0x033771e846f8925ad8251408abb76609acbd1fb0 holds 0.03349214591877884 LP tokens

0x0000000000000000000000000000000000000000 holds 1e-15 LP tokens

Security Analysis
-----------------
⚠️ Suspicious URL(s) found in the contract.
  URL: https://github.com/ethereum/solidity/issues/2691, Reason: metamask recovery phrase phishing
  URL: https://github.com/ethereum/EIPs/issues/20#issuecomment-263524729, Reason: metamask recovery phrase phishing
  URL: https://github.com/OpenZeppelin/openzeppelin-contracts/pull/522, Reason: metamask recovery phrase phishing
  URL: https://eips.ethereum.org/EIPS/eip-1884[EIP1884], Reason: metamask recovery phrase phishing
  URL: https://diligence.consensys.net/posts/2019/09/stop-using-soliditys-transfer-now/[Learn, Reason: metamask recovery phrase phishing
  URL: https://solidity.readthedocs.io/en/v0.5.11/security-considerations.html#use-the-checks-effects-interactions-pattern[checks-effects-interactions, Reason: metamask recovery phrase phishing
  URL: https://solidity.readthedocs.io/en/latest/units-and-global-variables.html?highlight=abi.decode#abi-encoding-and-decoding-functions[`abi.decode`]., Reason: metamask recovery phrase phishing

Lifecycle Analysis
-------------
Token Age: 1518.60 days
Time to First Trade: 0.00 hours
Blocks to First Trade: 0
Token Creation Date: 2021-06-07T13:06:04
Last Active: 2024-05-16T15:42:10
Days Since Last Activity: 444.4887359223495 days
Last Transaction Hash: 0xdca31575fe583f338a216e3bcdd1785fd91691c304cd0df4879e42cd3cbd4949
